import { TableRow } from "@/types/type";
interface TableDetailsProps {
  user: TableRow[];
  onClose: () => void;
}
export default function TableDetails(TableDetails: TableDetailsProps) {
  return <div>TableDetails</div>;
}
